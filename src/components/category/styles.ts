import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
   conteiner: {
      flexDirection: "row",
      alignItems: 'center',
      gap: 5
   },
   name: {
      fontSize: 16,
      fontWeight: '600',
   }
})